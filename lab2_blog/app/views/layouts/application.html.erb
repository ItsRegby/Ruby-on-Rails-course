<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Blog</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <!-- Blog link on the left -->
    <%= link_to root_path, class: 'navbar-brand', data: { turbo: true } do %>
      <i class="bi bi-book-half me-2"></i>My Blog
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- All other elements on the right -->
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to root_path, class: 'nav-link', data: { turbo: true } do %>
            <i class="bi bi-house-door me-1"></i>Home
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to categories_path, class: 'nav-link', data: { turbo: true } do %>
            <i class="bi bi-collection me-1"></i>Categories
          <% end %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to profile_path, class: 'nav-link', data: { turbo: true } do %>
              <i class="bi bi-person-circle me-1"></i>Profile
            <% end %>
          </li>
        <% end %>

        <li class="nav-item ms-2">
          <% if user_signed_in? %>
            <%= button_to destroy_user_session_path, method: :delete, class: 'btn btn-outline-danger', form_class: 'd-inline-block' do %>
              <i class="bi bi-box-arrow-right me-1"></i>Log out
            <% end %>
          <% else %>
            <%= link_to new_user_registration_path, class: 'btn btn-outline-light me-2' do %>
              <i class="bi bi-person-plus me-1"></i>Sign up
            <% end %>
            <%= link_to new_user_session_path, class: 'btn btn-primary' do %>
              <i class="bi bi-box-arrow-in-right me-1"></i>Log in
            <% end %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container mt-4">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>

<!-- Bootstrap JS and dependencies -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>